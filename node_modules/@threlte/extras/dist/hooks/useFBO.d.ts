import { type RenderTargetOptions, WebGLRenderTarget } from 'three';
interface UseFBOOptions extends RenderTargetOptions {
    /** Defines the count of MSAA samples. Can only be used with WebGL 2. Default: 0 */
    samples?: number;
    /** If set, the scene depth will be rendered into buffer.depthTexture. Default: false */
    depth?: boolean;
}
export declare const useFBO: (width?: number | UseFBOOptions, height?: number, options?: UseFBOOptions) => WebGLRenderTarget;
export {};
