import { BufferGeometry } from "three";
/**
 * Just a bare minimum geometry used to render the flipbook.
 */
export declare class ClippedFlipbookGeometry extends BufferGeometry {
    constructor(vertices: number);
}
